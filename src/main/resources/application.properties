spring.application.name=SpringDataJPA-P01


spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/studentDb
spring.datasource.username=root
spring.datasource.password=Paras@12345
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true


#To create the scheme in the database, this line tells that run this code in respective of the MySql
#this automatically created using the drivers, but we are just helping the framework that convert the
#syntax in terms of the mySql
#spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect




#Property that can create the db if db does not exist
#
#spring.datasource.url=jdbc:mysql://localhost:3306/studentDb?createDatabaseIfNotExist=true
#
#how internally this work ?
#sql:     IF NOT EXISTS studentDb THEN
#            CREATE DATABASE studentDb;
#         END IF;
#
#✔ DB created only if it does NOT exist
#✔ Safe even when DB already exists
#

